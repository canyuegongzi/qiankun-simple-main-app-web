import React from "react";
import MainView from "./MainView";

const MainRootView = function (props) {
    return (
        <MainView {...props}></MainView>
    )
}
export default MainRootView;
